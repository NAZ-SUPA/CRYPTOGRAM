<resources>
    <!--
        App Design System & Theming Logic:
        - This file defines the global visual language of the CRYPTOGRAM app.
        - Styles are grouped by their functional role (Branding, Navigation, UI Controls).
        - Using styles ensures visual consistency and allows for "one-change-everywhere" updates.
    -->

    <!-- Base application theme: Uses Material3 DayNight for future dark mode support. -->
    <style name="Base.Theme.CRYPTOGRAM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Brand colors and baseline Material components configuration. -->
    </style>

    <!-- Main Theme Alias -->
    <style name="Theme.CRYPTOGRAM" parent="Base.Theme.CRYPTOGRAM" />

    <!--
        BRANDING STYLES:
        Used on the Splash (MainActivity) and MainMenu screens to establish the identity.
    -->
    <style name="welcome_to">
        <item name="android:textSize">35sp</item>
        <item name="android:textColor">@color/purple_vibrant</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="cryptogram">
        <item name="android:textSize">48sp</item>
        <item name="android:letterSpacing">0.04</item>
        <item name="android:textColor">@color/purple_vibrant</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="cipher_style">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/purple_vibrant</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

    <!--
        MENU & NAVIGATION STYLES:
        Logic: Chips and Buttons use high-contrast colors (Black/White/Purple)
        to guide the user's focus towards primary actions.
    -->
    <style name="focus">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/iceberg</item>
    </style>

    <style name="english" parent="focus" />
    <style name="puzzle" parent="focus" />

    <style name="play">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/main_blue</item>
        <item name="android:fontFamily">@font/iceberg</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="levels" parent="play" />

    <!--
        GAMEPLAY UI STYLES:
        Logic: Mistakes use red to signify warning/error state.
        Keyboard keys use a uniform height and weight for a balanced QWERTY grid.
    -->
    <style name="mistakes">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/error_red</item>
        <item name="android:fontFamily">@font/iceberg</item>
        <item name="android:letterSpacing">0.05</item>
    </style>

    <style name="KeyboardKey">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:background">@drawable/key_background_bold</item>
        <item name="backgroundTint">@null</item> <!-- Required to allow custom drawable background -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">0dp</item>
    </style>

    <!-- UI Utility Styles -->
    <style name="full">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/inter_bold</item>
    </style>

</resources>