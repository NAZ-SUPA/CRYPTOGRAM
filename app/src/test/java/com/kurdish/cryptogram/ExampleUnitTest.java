package com.kurdish.cryptogram;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Host-side unit test scaffold:
 * - Runs on local JVM (no Android device/emulator required).
 * - Validates pure Java/Kotlin logic quickly during development.
 * - This placeholder assertion confirms test wiring is healthy.
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        // Sanity check: verifies JUnit executes and project test task is configured correctly.
        assertEquals(4, 2 + 2);
    }
}